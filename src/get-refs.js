const input = document.getElementById("search");
const listResults = document.getElementById("result_list");
const detailResults = document.getElementById("result_detail");
export default {
    input: input,
    listResults: listResults,
    detailResults: detailResults
};
